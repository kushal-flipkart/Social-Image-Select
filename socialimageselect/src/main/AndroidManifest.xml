<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="xyz.kushal.socialimageselect">

    <application
        android:name="xyz.kushal.utils.ApplicationController"
        android:allowBackup="true"
        android:label="@string/app_name">

        <activity
            android:name=".PhotoAlbumActivity"
            android:label="@string/title_activity_photo_album"
            android:screenOrientation="portrait"
            android:theme="@style/LibThemeNoActionBar" />

        <activity
            android:name=".SelectPhotoActivity"
            android:label="@string/select_photos"
            android:screenOrientation="portrait"
            android:theme="@style/AppThemeWithActionBar" />
    </application>

</manifest>
